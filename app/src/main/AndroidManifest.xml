<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cratisspace.avinashbadramoni.wewriteone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreenActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".InterShowCaseActivity" />
        <activity android:name=".ShowSingle" />
        <activity android:name=".cheActivity" />
        <activity android:name=".boActivity" />
        <activity android:name=".zoActivity" />
        <activity android:name=".messageActivity" />
        <activity android:name=".AdsActivity" />
        <activity android:name=".ScratchCardActivity" />
        <activity android:name=".supportActivity" />
        <activity android:name=".AnyRecordActivity"></activity>
    </application>

</manifest>